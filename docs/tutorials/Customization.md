# 🛠️ 自订

BibleMate AI 具有高度可自订性。进阶使用者可以修改现有工具、建立新工具，甚至透过自订系统提示来改变 AI 代理的行为。

自订档案放置在 AgentMake 使用者目录中的 `biblemate` 子目录中（在 Linux/macOS 上通常是 `~/.agentmake/biblemate`，在 Windows 上是 `%USERPROFILE%\.agentmake\biblemate`）。

## 主要设定档

主要设定储存在以下档案中：

`~/.agentmake/biblemate/biblematesc.config` (在 Linux/macOS 上)

`%USERPROFILE%\.agentmake\biblemate\biblematesc.config` (在 Windows 上)

您可以手动编辑此档案以更改某些设定。

例如，您可以透过更改此档案中 `color_agent_mode` 和 `color_partner_mode` 的值来自订代理和伙伴模式的边框颜色。

## 常用提示和计划

您可以储存、搜寻、开启或删除常用提示和计划。

例如：

* 输入提示并使用 `Ctrl+W` 储存提示。
* 在提示前加上 `@@` 并使用 `Ctrl+W` 储存提示。
* 使用 `Esc+W` 删除已储存的提示/计划。
* 使用 `Ctrl+L` 开启提示/计划。
* 使用 `Esc+L` 搜寻提示/计划。

## 覆写系统提示

代理的核心逻辑由系统提示（markdown 档案）引导。您可以透过将自己的版本放置在 `~/.agentmake/systems/biblematesc/` 中来覆写它们。

可自订的系统提示档案是：`supervisor.md`、`tool_instruction.md` 和 `tool_selection.md`。您可以将它们从套件安装资料夹中的 `biblematesc/systems` 目录复制到您的使用者目录并根据需要进行修改。

## 新增或修改工具和计划

您可以透过建立自订的 `bible_study_mcp.py` 档案来新增自己的工具和内建计划（提示）。

1. 首先，找到 `biblematesc` 套件安装目录中的内建 `bible_study_mcp.py` 档案。
2. 将此档案复制到您的使用者自订目录 `~/.agentmake/biblematesc/bible_study_mcp.py`。
3. 现在您可以编辑此档案，使用 `fastmcp` 语法新增或修改工具和提示。BibleMate AI 将自动载入您的自订档案而不是内建档案。

## 使用 http 作为传输而不是 stdio

BibleMate 使用 `stdio` 作为与 BibleMate MCP 伺服器互动的预设传输。您可以改用 `http`。

在一个执行绪中运行：

> biblematemcp

在另一个执行绪中运行：

> biblemate -mcp biblemate

## 使用本地圣经资料

阅读 [此处](https://github.com/eliranwong/biblematesc/issues/15#issuecomment-3314130281) 以获取更多详细资讯。

## 使用自订 MCP 伺服器

您可以透过 CLI 选项 `mcp` 使用自订 MCP 伺服器，例如：

> biblemate -mcp http://127.0.0.1:33333/mcp

## 托管或运行 BibleMate MCP 伺服器

使用预设埠 `33333`：

> biblematemcp

预设埠可以在设定档 `config.py` 中编辑。

要暂时覆写预设埠，例如：

> biblematemcp -p 33334
